"""Passive public observation.

The lawful path to a domain nobody has claimed: read what the domain already publishes,
and nothing more. Operations are restricted by an allowlist rather than by convention,
so an unauthorized run cannot reach an authorization-gated capability.
"""
